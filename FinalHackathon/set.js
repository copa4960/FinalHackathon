/*var prev = chrome.storage.local.get('key');

prev.addEventListener("click", function(){
 
    var li = document.createElement("li");
    var text = document.createTextNode(prev);
    li.appendChild(text);
    document.getElementById("unordered").appendChild(li);
    
});*/